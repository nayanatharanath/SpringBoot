package com.example.student.controller;

import static org.mockito.Mockito.mock;

import java.util.List;

import org.assertj.core.util.Arrays;
import org.junit.Test;

import com.example.student.model.Student;
import com.example.student.service.StudentServImpl;

public class StudentControllerTest {

	private final StudentServImpl mockService = mock(StudentServImpl.class);
	// private final StudentController studentController = new
	// StudentController(mockService);

}
